# Herdr runtime sockets
*.sock

# Logs
*.log

# Runtime / session state (machine-specific, regenerated by herdr)
session.json
plugins.json

# Herdr-managed plugin downloads (each has its own .git + build artifacts),
# runtime copies & per-plugin config/state. Reinstall via `herdr plugin install`.
plugins/github/
plugins/config/

# OS noise
.DS_Store
.envrc
release-notes.json
